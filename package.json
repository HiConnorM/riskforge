{
  "name": "riskforge",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm -r dev",
    "deps:up": "docker compose -f ops/docker-compose.yml up -d",
    "deps:down": "docker compose -f ops/docker-compose.yml down",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
